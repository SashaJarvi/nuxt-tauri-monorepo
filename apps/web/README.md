# apps/web

The Nuxt 4 web app. Extends the shared `packages/ui` Nuxt layer via `extends: ["@repo/ui"]` in `nuxt.config.ts` — most feature code lives there, not here. See the root [README.md](../../README.md) for setup, scripts, and architecture.
